#!/usr/bin/env node
/**
 * Lass CLI entry point
 *
 * This is the executable entry point for the `lass` command.
 * All logic is in index.ts - this just imports and runs main().
 */

import { main } from '../index.js';

main();
